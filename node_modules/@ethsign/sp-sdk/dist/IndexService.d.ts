import { AttestationInfo, PageInfo, SchemaInfo } from './types/indexService';
export declare class IndexService {
    private host;
    constructor(env: 'mainnet' | 'testnet');
    querySchemaList: (query: {
        id?: string;
        registrant?: string;
        mode?: 'offchain' | 'onchain';
        page: number;
        size?: number;
    }) => Promise<(PageInfo & {
        rows: SchemaInfo[];
    }) | null>;
    querySchema: (schemaId: string) => Promise<SchemaInfo | null>;
    queryAttestationList: (query: {
        id?: string;
        schemaId?: string;
        attester?: string;
        page: number;
        mode?: 'offchain' | 'onchain';
        indexingValue?: string;
    }) => Promise<(PageInfo & {
        rows: AttestationInfo[];
    }) | null>;
    queryAttestation: (attestationId: string) => Promise<AttestationInfo | null>;
}

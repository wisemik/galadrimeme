import { DataLocationOnChain } from './types';
export declare const getDataFromStorage: (data: {
    dataId: string;
    dataLocation: DataLocationOnChain.ARWEAVE | DataLocationOnChain.IPFS;
}) => Promise<any>;
